const PREFIX = "study-hub_";

export const STORAGE_KEYS = {
    subjects: `${PREFIX}subjects`,
    units: `${PREFIX}units`,
    contents: `${PREFIX}contents`,
    nodes: `${PREFIX}nodes`,
    exams: `${PREFIX}exams`,
};
